# x-release-please-component-version python
name: Build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    name: Python Build
    uses: sassy-bulldog/.github/.github/workflows/build-python.yml@python-v1.0.0 # x-release-please-component-version python
    with:
      runs-on: '["ubuntu-latest"]'
      artifact-name: 'python-build-artifacts'
      version: ${{ github.ref_type == 'tag' && github.ref_name || '0.0.1-alpha' }}
