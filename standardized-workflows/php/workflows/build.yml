# x-release-please-component-version php
name: Build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    name: PHP Build
    uses: sassy-bulldog/.github/.github/workflows/build-php.yml@v0.2.2 # x-release-please-component-version php
    with:
      runs-on: '["ubuntu-latest"]'
      artifact-name: 'php-build-artifacts'
      version: ${{ github.ref_type == 'tag' && github.ref_name || '0.0.1-alpha' }}
