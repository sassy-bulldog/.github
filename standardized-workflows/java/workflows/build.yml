# x-release-please-component-version java
name: Build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    name: Java Build
    uses: sassy-bulldog/.github/.github/workflows/build-java.yml@v0.2.2 # x-release-please-component-version java
    with:
      runs-on: '["ubuntu-latest"]'
      artifact-name: 'java-build-artifacts'
      version: ${{ github.ref_type == 'tag' && github.ref_name || '0.0.1-alpha' }}
